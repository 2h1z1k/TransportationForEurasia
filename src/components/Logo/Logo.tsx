import React from 'react'
import logo from 'images/logo.png'

type Props = {}

const Logo = (props: Props) => {
    return (
        <>
            <img src={logo} />
        </>
    )
}

export default Logo
